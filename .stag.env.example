API_KEY=123456
DATABASE_NAME=my_db_stag
DATABASE_PORT=8091
PORT=3000

POSTGRES_DB=/* db name on local postgres instance */
POSTGRES_USER= /* postgres user on local instance */
POSTGRES_PASSWORD= /* local instance password */
POSTGRES_PORT= /* port of the local instance, by default postgres runs on port 5432 */
POSTGRES_HOST= /* localhost, default is localhost or 127.0.0.1 */